[package]
name = "vardalith"
version = "0.1.0"
edition = "2024"

[dependencies]
addr2line = "0.25.0"
clap = "4.5.46"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"

[target.'cfg(unix)'.dependencies]
libc = "0.2"
